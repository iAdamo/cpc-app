import { VStack } from "@/components/ui/vstack";
import { Text } from "@/components/ui/text";

const ViewProposal = () => {
  return (
    <VStack>
      <Text>View Proposal Screen</Text>
    </VStack>
  );
};

export default ViewProposal;
